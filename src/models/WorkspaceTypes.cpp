// The core data-model value types (SPEC 3.1-3.4) are aggregates declared inline
// in WorkspaceTypes.h with defaulted equality. This translation unit compiles
// the header as part of ch_models and guards that it is self-contained.
#include "WorkspaceTypes.h"
